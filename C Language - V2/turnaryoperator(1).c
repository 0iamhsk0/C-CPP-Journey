#include <stdio.h>
main()
{
	int age,voter_id;
	printf("Voter, pls enter your age:\n ");
	scanf("%d",&age);
	(age >= 18)?printf("Candidate is eligible for voting!"):("Candidate is not eligible for voting!!");
}

//write a program to find thew greater no, from 3 given no.
